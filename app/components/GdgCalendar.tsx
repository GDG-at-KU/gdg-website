"use client";
import { useState } from "react";
const months=["September 2026","October 2026","November 2026"]; const events:{[key:string]:string}={"0-5":"Build Night","0-19":"Tech Talk","1-3":"Hack Day"};
export function GdgCalendar(){const [month,setMonth]=useState(0);const [chosen,setChosen]=useState("0-5");const start=[2,4,0][month],days=[30,31,30][month];const cells=Array.from({length:start+days},(_,i)=>i<start?0:i-start+1);const key=`${month}-${chosen.split("-")[1]||5}`;return <section className="calendar-shell"><div className="calendar-head"><p>GDG ON CAMPUS · KU</p><a href="/events">← All events</a></div><div className="calendar-title"><div><p className="eyebrow"><span/> EVENT CALENDAR</p><h1>Make room for<br/><i>what’s next.</i></h1></div><div className="calendar-controls"><button onClick={()=>setMonth(Math.max(0,month-1))} disabled={!month}>←</button><b>{months[month]}</b><button onClick={()=>setMonth(Math.min(2,month+1))} disabled={month===2}>→</button></div></div><div className="calendar-layout"><div className="calendar-grid"><div className="weekdays">{["SUN","MON","TUE","WED","THU","FRI","SAT"].map(day=><span key={day}>{day}</span>)}</div><div className="days">{cells.map((day,i)=>{const id=`${month}-${day}`;const event=events[id];return <button className={`${event?"event-day":""} ${chosen===id?"selected":""}`} disabled={!day} onClick={()=>setChosen(id)} key={i}><b>{day||""}</b>{event&&<i>{event}</i>}</button>})}</div></div><aside className="calendar-detail"><p>SELECTED DATE</p><h2>{events[chosen]||"Open for ideas"}</h2><span>{chosen==="0-5"?"September 5 · 6:00 PM":chosen==="0-19"?"September 19 · 5:30 PM":chosen==="1-3"?"October 3 · All day":"No scheduled event"}</span><hr/><p>{events[chosen]?"KU CAMPUS · RSVP REQUIRED":"Want to host a session? Let the team know."}</p><a href="/join">Join the community ↗</a></aside></div></section>}
